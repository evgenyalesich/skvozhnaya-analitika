# Frontend

React + Vite frontend for MVP Overview/RAW Users.

## Setup
1. `cd frontend`
2. `npm install`
3. `npm run dev`

The frontend expects backend on `http://localhost:8000`. Update `hooks/useReports.ts` and `hooks/useBots.ts` if API runs elsewhere.
