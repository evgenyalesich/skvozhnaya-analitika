# Конфигурации

Здесь будут YAML-файлы:
- `bots.yaml` (каждый бот теперь указывает `database_name` вместо DSN-ссылки; actual DSN формируется из `POSTGRES_ADMIN_DSN`)
- `advertising_companies.yaml`
- `data_sources.yaml`
